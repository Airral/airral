name: Backend Security Scan

on:
  schedule:
    - cron: '0 2 * * 1' # Weekly on Monday at 2 AM
  push:
    branches: [ main ]
    paths:
      - 'airral-backend/**'
  workflow_dispatch:

defaults:
  run:
    working-directory: airral-backend
