package com.airral.domain.enums;

public enum FeedVisibility {
    PUBLIC,              // Visible to everyone (candidates, applicants, employees)
    AUTHENTICATED,       // Visible to logged-in users only
    APPLICANTS_ONLY      // Visible to applicants/candidates only
}
