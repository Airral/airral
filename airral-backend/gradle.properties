# Do NOT set org.gradle.java.home here: it is machine-specific and breaks CI and
# the Docker build. The JDK is pinned by the Java toolchain in build.gradle.
# If you need to override it locally, use ~/.gradle/gradle.properties instead.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
